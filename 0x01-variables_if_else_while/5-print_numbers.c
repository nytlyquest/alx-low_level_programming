#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int baseTen;

	for (baseTen = 0; baseTen < 10; baseTen++)
		printf("%d", baseTen);
	printf("\n");
	return (0);
}
