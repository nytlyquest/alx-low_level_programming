#include "main.h"

/**
 * mul - calculates the product of 2 numbers
 * @a: the first product number
 * @b: the second product number
 * Return: Always 0
 */
int mul(int a, int b)
{
	return (a * b);
}
